- hosts: k8s
  user: ec2-user
  tasks:
    - name: Update Apps
      shell: |
        git clone https://github.com/fmaulana240699/Automation.git
        cd Automation
        kubectl apply -f deployment.yaml
        cd ..
        rm -rf Automation
